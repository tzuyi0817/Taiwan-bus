import { divIcon } from 'leaflet';

export const SELF_MARKER = divIcon({
  className:'marker_self',
  html:`<div></div>`
});

export const STOP_MARKER = divIcon({
  className:'marker_stop',
  html:`<div></div>`
});

export const PIT_MARKER = divIcon({
  className:'maker_stop_pit',
  html:`<div></div>`
});

export const GREEN_MARKER = divIcon({
  className:'marker_green',
  html:`<div></div>`
});
